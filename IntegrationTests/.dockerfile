# Start Database, add necessary data into the DB
# Start Backend Service, conenct to this db
# hit backend service localhost:xxxx / endpoint, assert output

# could even be a python script like, requests.fetch
